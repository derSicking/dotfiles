# Mouse binding: inspect window and insert a rule template
# Mod4 (Win/Super) + Ctrl + right-click on any window
# Captures window info BEFORE opening a terminal (avoids focus-stealing race)
bindsym --whole-window $mod+Ctrl+button3 focus; exec sway-window-rule
