#!/usr/bin/env bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

cd "$SCRIPT_DIR"

# Run all 'setup' scripts
echo "Runing all setup scripts..."
find -name "setup" -exec "{}" \;
echo -e "Setups done\n"

# Run all 'link' scripts
echo "Runing all link scripts..."
find -name "link" -exec "{}" \;
echo -e "Links done\n"
